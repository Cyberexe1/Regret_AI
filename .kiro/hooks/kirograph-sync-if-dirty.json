{
  "version": "v1",
  "hooks": [
    {
      "name": "KiroGraph Sync on Agent Stop",
      "trigger": "Stop",
      "description": "Syncs the KiroGraph knowledge graph after each agent run. No shell redirection: the command runs under cmd.exe, where `*>` is not redirection and the asterisk is passed to kirograph as a target path. --quiet already suppresses output.",
      "action": {
        "type": "command",
        "command": "kirograph sync --quiet"
      }
    }
  ]
}