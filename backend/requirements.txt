fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.13.5
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
boto3==1.35.97
botocore==1.35.97
python-multipart==0.0.20
pypdf==5.1.0
python-docx==1.1.2
pytest==8.3.4
pytest-asyncio==0.25.2
moto[dynamodb]==5.0.28
ruff==0.8.6

# --- AI agents (Strands Agents SDK on Amazon Bedrock) ------------------------
# strands-agents transitively requires a newer `starlette` (via its `mcp`
# dependency) than earlier FastAPI pins allowed, which is why fastapi was
# bumped above from 0.115.x to 0.141.x - not a discretionary upgrade.
strands-agents==1.50.2
